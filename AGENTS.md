## 行为边界

默认只回答用户的问题，不主动执行操作。
只有当用户明确要求执行任务时，才修改代码、运行命令或创建文件。
如果意图不明确，那就是在问问题。
对于单纯提问，直接、快速回答，不启动任务执行流程。
写代码时，添加必要的注释，方便人类阅读理解。
spec 是人和 agent 共同参考的，所以要方便人 review，不能拉屎，只需简洁写好已确认信息，别的不用写入

## Git

Git commit 信息使用中文。

## 开发原则

项目目前处于探索和学习阶段，优先保持简单、清晰和易于 review。

只实现当前任务真正需要的内容，不提前实现未来可能需要的功能，也不要顺手补充额外工程化内容。

可以预留目录和结构，但如果当前没有实际需求，不要提前填充实现。

当存在多种方案时，优先选择改动更小、概念更少、容易理解和调整的方案。