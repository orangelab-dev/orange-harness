# orange-harness 使用教程

## 1. 准备环境

需要：

- Python 3.12 或更高版本
- [uv](https://docs.astral.sh/uv/)
- DeepSeek API Key

克隆并进入项目：

```bash
git clone https://github.com/orangelab-dev/orange-harness.git
cd orange-harness
```

## 2. 配置 API

创建用户级配置目录，再复制示例：

```bash
mkdir -p ~/.config/orange-harness
cp .env.example ~/.config/orange-harness/.env
```

编辑 `~/.config/orange-harness/.env`，至少填写：

```dotenv
DEEPSEEK_API_KEY=你的真实 API Key
```

其余配置可以先保持默认：

```dotenv
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

系统环境变量的优先级高于配置文件。程序优先读取 `~/.config/orange-harness/.env`；这个文件不存在或无法读取时，才回退读取启动目录下的 `.env`。

## 3. 安装命令行

在 `orange-harness` 项目目录执行一次：

```bash
uv tool install --editable .
```

安装完成后会得到命令：

```bash
orange-harness --approval policy
```

`--editable` 表示命令继续使用当前项目源码。以后修改 Python 代码，不需要反复安装。

## 4. 在一个 workspace 中启动

先进入希望 Agent 操作的目录，再启动：

```bash
cd /path/to/your/workspace
orange-harness
```

例如：

```bash
cd ~/Projects/demo
orange-harness
```

启动命令所在的目录就是 workspace：

```text
Agent workspace：~/Projects/demo
Shell 执行目录：~/Projects/demo
运行日志：~/Projects/demo/logs/agent.log
```

输入问题即可开始，输入下面任意一个命令退出：

```text
exit
quit
```

## 5. 选择运行模式

Sandbox 决定命令在哪里执行，Approval 决定命令能不能执行，两者相互独立。

六种组合如下：

```bash
# MacOSSandbox + 默认拒绝 ask
orange-harness

# MacOSSandbox + 人工确认 ask（推荐日常使用）
orange-harness --approval policy

# MacOSSandbox + 自动执行 ask
orange-harness --approval auto

# NoSandbox + 默认拒绝 ask
orange-harness --unsafe

# NoSandbox + 人工确认 ask
orange-harness --unsafe --approval policy

# NoSandbox + 自动执行 ask（风险最高）
orange-harness --unsafe --approval auto
```

`--unsafe` 表示 Shell 直接在宿主机执行，没有系统级隔离。Linux、Windows 或 macOS 沙箱不可用时，只有显式传入它才能执行 Shell。

## 6. Tool 审批

Tool 有三种审批结果：

```text
allow → 自动执行
ask   → 由全局审批模式决定
deny  → 直接拒绝
```

全局审批模式只处理 `ask`：

| Tool 决定 | 默认 `deny` | `policy` | `auto` |
|---|---|---|---|
| `allow` | 执行 | 执行 | 执行 |
| `ask` | 拒绝 | 人工确认 | 执行 |
| `deny` | 拒绝 | 拒绝 | 拒绝 |

因此 `auto` 也不能绕过 Tool 明确给出的 `deny`。

需要确认时，终端会显示：

```text
[approval]
cwd: /path/to/your/workspace
tool: shell
arguments: {"command": "python script.py"}
是否执行？[y/N]:
```

只有输入 `y` 或 `yes` 才会执行。直接回车或输入其他内容都会拒绝。

## 7. Shell 沙箱

### macOS

macOS 默认使用系统的 `sandbox-exec`：

- workspace 外禁止写入。
- workspace 外允许读取。
- workspace 内允许读写。
- 网络默认禁止。

沙箱不可用或启动失败时，命令不会偷偷改成裸执行，而会返回：

```text
sandbox_unavailable
```

沙箱仍然允许修改和删除 workspace 内的内容，因此执行前仍应检查命令。

### Linux、Windows 和其他平台

当前没有实现这些平台的系统沙箱，所以 Shell 默认不可执行。

只有明确接受“命令直接在宿主机运行”的风险时，才传入 `--unsafe`。这会启用 `NoSandbox`；Tool 的 `allow / ask / deny` 审批仍然有效。

## 8. 不安装 CLI，临时运行

如果只想在源码仓库中运行：

```bash
uv sync
uv run orange-harness --approval policy
```

这种方式会把源码仓库本身作为 workspace。测试 Shell Tool 时，应注意它可能修改当前项目文件。

## 9. 常见问题

### 找不到 `orange-harness` 命令

先确认安装成功：

```bash
uv tool list
```

如果 uv 提示工具目录不在 `PATH`，按照 uv 输出的提示配置终端环境，然后重新打开终端。

### 缺少 API Key

确认系统环境变量、`~/.config/orange-harness/.env` 或当前 workspace 的 `.env` 包含：

```dotenv
DEEPSEEK_API_KEY=你的真实 API Key
```

### Shell 返回 `sandbox_unavailable`

- macOS：确认系统存在 `/usr/bin/sandbox-exec`。
- 其他平台：当前只能显式传入 `--unsafe`，或者暂时不使用 Shell Tool。

### 查看完整运行信息

打开当前 workspace 下的：

```text
logs/agent.log
```

文件日志会保存完整模型响应、Tool 调用、审批事件和执行结果，可能包含对话或命令中的敏感信息，不要直接公开分享。
